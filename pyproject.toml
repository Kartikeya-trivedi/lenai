[project]
name = "len-ai"
version = "1.0.0"
description = "Production Media Inference API Platform — unified REST API for image and voice inference"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
